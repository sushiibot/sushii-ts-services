query userGlobalLevel($userId: BigInt!) {
  allUserLevels(condition: { userId: $userId }) {
    aggregates {
      sum {
        msgAllTime
        msgDay
        msgMonth
        msgWeek
      }
    }
    nodes {
      lastMsg
    }
  }
}
