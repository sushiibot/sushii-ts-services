mutation deleteRoleMenuRoles(
  $guildId: BigInt!
  $menuName: String!
  $roleIds: [BigInt]!
) {
  deleteRoleMenuRoles(
    input: { menuName: $menuName, roleIds: $roleIds, guildId: $guildId }
  ) {
    roleMenuRoles {
      ...RoleMenuRoleData
    }
  }
}
