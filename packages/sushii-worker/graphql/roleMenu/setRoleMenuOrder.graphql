mutation setRoleMenuRoleOrder(
  $guildId: BigInt!
  $menuName: String!
  $roleIds: [BigInt]!
) {
  setRoleMenuRoleOrder(
    input: { guildId: $guildId, menuName: $menuName, roleIds: $roleIds }
  ) {
    roleMenuRoles {
      ...RoleMenuRoleData
    }
  }
}
