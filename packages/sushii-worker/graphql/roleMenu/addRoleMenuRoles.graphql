mutation addRoleMenuRoles(
  $guildId: BigInt!
  $menuName: String!
  $roleIds: [BigInt]!
) {
  addRoleMenuRoles(
    input: { guildId: $guildId, menuName: $menuName, roleIds: $roleIds }
  ) {
    roleMenuRoles {
      ...RoleMenuRoleData
    }
  }
}
