mutation addXpBlock(
  $guildId: BigInt!
  $blockId: BigInt!
  $blockType: BlockType!
) {
  createXpBlock(
    input: {
      xpBlock: { guildId: $guildId, blockId: $blockId, blockType: $blockType }
    }
  ) {
    xpBlock {
      ...XpBlockData
    }
  }
}
