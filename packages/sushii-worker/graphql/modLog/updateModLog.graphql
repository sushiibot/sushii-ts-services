mutation updateModLog(
  $caseId: BigInt!
  $guildId: BigInt!
  $modLogPatch: ModLogPatch!
) {
  updateModLogByGuildIdAndCaseId(
    input: { modLogPatch: $modLogPatch, guildId: $guildId, caseId: $caseId }
  ) {
    modLog {
      ...ModLogData
    }
  }
}
