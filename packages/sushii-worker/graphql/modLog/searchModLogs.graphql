query searchModLogs($guildId: BigInt!, $searchCaseId: BigInt!) {
  searchModLogs(
    guildId: $guildId
    searchCaseId: $searchCaseId
    maxResults: 25
  ) {
    nodes {
      ...ModLogData
    }
  }
}
