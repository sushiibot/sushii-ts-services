query getRedisGuild($guild_id: BigInt!) {
  redisGuildByGuildId(guild_id: $guild_id) {
    id
    roles {
      id
      color
      hoist
      icon
      managed
      mentionable
      position
      name
      permissions
      unicode_emoji
    }
  }
}
